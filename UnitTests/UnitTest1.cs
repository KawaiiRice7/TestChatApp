using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ChatroomUnitTesting
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //    var connectionHelper = ObjectFactory.Resolve<IConnectionHelper>();
            //    var chatAccessor = ObjectFactory.Resolve<IChatAccessor>();
            //    var credentialAccessor = ObjectFactory.Resolve<ICredentialAccessor>();

            //    User user1 = new User("B276CA1D-92E4-4B9C-9A3B-A410807573D9", "KawaiiRice", "FakePw");
            //    User user2 = new User("999", "Fake", "ReallyFakePw");

            //    Message message = new Message() { Id = 1, Author = user1, Content = $"Unit test message at: {DateTime.Now}", Date = DateTime.Now };

            //    var result = await chatAccessor.GetMessages(10);
            //    await chatAccessor.PostMessage(message);

            //    //await credentialAccessor.CreateAccount(user1.Username, user1.Password);
            //    //await credentialAccessor.CreateAccount(user1.Username, user1.Password);

            //    //await credentialAccessor.CreateAccount(user2.Username, user2.Password);

            //    //await credentialAccessor.VerifyCredential(user1.Username, user1.Password);
            //    //await credentialAccessor.VerifyCredential(user2.Username, user2.Password);



            //    //await connectionHelper.OpenSocket(80);
            //    connectionHelper.StartListening(80);
            //    //await connectionHelper.OpenSocket(90);


            //    Console.WriteLine("Finished unit testing.");
        }
    }
}
